# 27. Write a Python program to concatenate all elements in a list into a string and return it.

separately = ['P', 'y', 't', 'h', 'o', 'n']
joined = "".join(separately)
print(joined)

"""
def laczenie (separately):
    wynik = ''
    for element in separately:
        wynik += str (element)
        print (wynik)
laczenie (separately)
"""
